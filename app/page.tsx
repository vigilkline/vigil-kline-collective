"use client";

import { ChangeEvent, FormEvent, useEffect, useMemo, useState } from "react";

type View = "dashboard" | "inventory" | "sessions" | "listings" | "orders" | "finances";
type Status = "Owned" | "Ready" | "Published" | "Sold";
type Item = { id: number; brand: string; description: string; cost: number; price: number; estimatedResale?: number; status: Status; photo?: string };
type Priority = { id: number; text: string; horizon?: "Today" | "Tomorrow" | "Later"; date?: string; category?: string; time?: string; done: boolean };
type TaxPayment = { id: number; amount: number; date: string };
type SessionPhase = "driving" | "parking" | "store";
type CandidateDecision = "undecided" | "bought" | "passed";
type SessionCandidate = Omit<Item,"status"> & { decision: CandidateDecision };
type ThriftHistory = { id:number; date:string; startedAt:number; endedAt:number; location:string; budget:number; spend:number; phaseTimes:Record<SessionPhase,number>; candidates:SessionCandidate[] };

const nav: { id: View; label: string; icon: string }[] = [
  { id: "dashboard", label: "Dashboard", icon: "⌂" }, { id: "inventory", label: "Inventory", icon: "▦" },
  { id: "sessions", label: "Sessions", icon: "◷" },
  { id: "listings", label: "Listings", icon: "▤" }, { id: "orders", label: "Orders", icon: "↗" },
  { id: "finances", label: "Finances", icon: "$" },
];
const money = (n: number) => `$${n.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
const localDate = (date = new Date()) => `${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,"0")}-${String(date.getDate()).padStart(2,"0")}`;
const openDb = () => new Promise<IDBDatabase>((resolve, reject) => { const q = indexedDB.open("vigilkline-local", 2); q.onupgradeneeded = () => { if (!q.result.objectStoreNames.contains("workspace")) q.result.createObjectStore("workspace"); }; q.onsuccess = () => resolve(q.result); q.onerror = () => reject(q.error); });
const read = async (key: string) => { const db = await openDb(); return new Promise<any>((resolve) => { const q = db.transaction("workspace").objectStore("workspace").get(key); q.onsuccess = () => resolve(q.result); q.onerror = () => resolve(undefined); }); };
const write = async (key: string, value: unknown) => { const db = await openDb(); return new Promise<void>((resolve,reject)=>{const tx=db.transaction("workspace","readwrite");tx.objectStore("workspace").put(value,key);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error);}); };

export default function Home() {
  const [view, setView] = useState<View>("dashboard");
  const [items, setItems] = useState<Item[]>([]);
  const [priorities, setPriorities] = useState<Priority[]>([]);
  const [taxRate, setTaxRate] = useState("0");
  const [taxPayments, setTaxPayments] = useState<TaxPayment[]>([]);
  const [sessions, setSessions] = useState<ThriftHistory[]>([]);
  const [sessionOpen, setSessionOpen] = useState(false);
  const [ready, setReady] = useState(false);
  const [toast, setToast] = useState("");
  useEffect(() => { Promise.all([read("items"), read("priorities"), read("tax-settings"),read("thrift-sessions")]).then(([a,b,t,s]) => { setItems(Array.isArray(a) ? a : []); setPriorities(Array.isArray(b) ? b : []); setSessions(Array.isArray(s)?s:[]); if(t&&typeof t==="object"){setTaxRate(String(t.rate??"0"));setTaxPayments(Array.isArray(t.payments)?t.payments:[]);} setReady(true); }).catch(() => setReady(true)); if ("serviceWorker" in navigator) navigator.serviceWorker.register("/sw.js").catch(()=>{}); }, []);
  useEffect(() => { if (ready) write("items", items).catch(()=>{}); }, [items, ready]);
  useEffect(() => { if (ready) write("priorities", priorities).catch(()=>{}); }, [priorities, ready]);
  useEffect(() => { if (ready) write("tax-settings", {rate:taxRate,payments:taxPayments}).catch(()=>{}); }, [taxRate, taxPayments, ready]);
  useEffect(() => { if (ready) write("thrift-sessions", sessions).catch(()=>{}); }, [sessions, ready]);
  const notify = (s:string) => { setToast(s); window.setTimeout(()=>setToast(""),2200); };
  const sold = items.filter(i=>i.status === "Sold");
  const revenue = sold.reduce((n,i)=>n+i.price,0), costs = items.reduce((n,i)=>n+i.cost,0);
  return <div className="shell">
    <aside><button className="brand" onClick={()=>setView("dashboard")}><span>V</span> VIGILKLINE</button><nav>{nav.map(n=><button key={n.id} className={view===n.id?"active":""} onClick={()=>setView(n.id)}><span>{n.icon}</span>{n.label}</button>)}</nav></aside>
    <main><header><button className="start" onClick={()=>setSessionOpen(true)}>◎ Start thrift session</button></header>
      {view==="dashboard" && <Dashboard items={items} priorities={priorities} setPriorities={setPriorities} setView={setView} start={()=>setSessionOpen(true)}/>} 
      {view==="inventory" && <Inventory items={items} setItems={setItems} start={()=>setSessionOpen(true)} notify={notify}/>} 
      {view==="sessions" && <PastSessions sessions={sessions}/>} 
      {view==="listings" && <Listings items={items} setItems={setItems} notify={notify}/>} 
      {view==="orders" && <Orders items={items} setItems={setItems} notify={notify}/>} 
      {view==="finances" && <Finances items={items} revenue={revenue} costs={costs} taxRate={taxRate} setTaxRate={setTaxRate} taxPayments={taxPayments} setTaxPayments={setTaxPayments}/>} 
    </main>
    <nav className="mobile-nav">{nav.map(n=><button key={n.id} className={view===n.id?"active":""} onClick={()=>setView(n.id)}><span>{n.icon}</span>{n.label}</button>)}</nav>
    {sessionOpen && <ThriftSession close={()=>setSessionOpen(false)} checkpoint={(session,newItems)=>{setSessions(v=>[session,...v]);setItems(v=>[...newItems,...v]);notify(`${newItems.length} bought ${newItems.length===1?"item":"items"} moved to inventory`);}} commit={(session,newItems)=>{setSessions(v=>[session,...v]);setItems(v=>[...newItems,...v]);setSessionOpen(false);setView("sessions");notify(`${newItems.length} bought ${newItems.length===1?"item":"items"} moved to inventory`);}}/>}
    {toast && <div className="toast">✓ {toast}</div>}
  </div>;
}

function Dashboard({items,priorities,setPriorities,setView,start}:any){
  const owned=items.filter((i:Item)=>i.status==="Owned").length, listings=items.filter((i:Item)=>["Ready","Published"].includes(i.status)).length, sold=items.filter((i:Item)=>i.status==="Sold");
  const profit=sold.reduce((n:number,i:Item)=>n+i.price-i.cost,0);
  return <div className="page"><section className="dashboard-head"><div><p className="eyebrow">OVERVIEW</p><h1>Dashboard</h1><button className="hero-start" onClick={start}>◎ Start thrift session <span>→</span></button></div><div className="balance-card"><small>RECORDED NET PROFIT</small><b>{money(profit)}</b><p>Sold price minus recorded item cost</p></div></section>
    <section className="metrics dashboard-metrics"><Metric label="Owned items" value={owned}/><Metric label="Orders" value={sold.length}/><Metric label="Net profit" value={money(profit)}/></section>
    <section className="dash-grid"><BrandCalendar priorities={priorities} setPriorities={setPriorities}/>
    <div className="panel pipeline"><div className="panel-head"><div><small>WORKFLOW</small><h2>Business at a glance</h2></div></div>{[["Inventory",owned,"Owned items"],["Listings",listings,"Ready or published"],["Orders",sold.length,"Sold items"],["Finances",money(profit),"Recorded net profit"]].map(([name,value,note])=><button key={String(name)} onClick={()=>setView(String(name).toLowerCase())}><span><b>{name}</b><small>{note}</small></span><strong>{value}</strong><i>→</i></button>)}</div></section>
  </div>;
}
function BrandCalendar({priorities,setPriorities}:any){
  const today=localDate();
  const [selected,setSelected]=useState(today), [month,setMonth]=useState(()=>new Date(`${today}T12:00:00`));
  const [text,setText]=useState(""), [category,setCategory]=useState(""), [time,setTime]=useState("");
  const [agenda,setAgenda]=useState<"day"|"week">("day"), [editing,setEditing]=useState<number|null>(null), [editText,setEditText]=useState("");
  const taskDate=(p:Priority)=>p.date||(p.horizon==="Tomorrow"?localDate(new Date(Date.now()+86400000)):p.horizon==="Later"?localDate(new Date(Date.now()+6*86400000)):today);
  const first=new Date(month.getFullYear(),month.getMonth(),1,12), start=new Date(first); start.setDate(1-first.getDay());
  const cells=Array.from({length:42},(_,i)=>{const d=new Date(start);d.setDate(start.getDate()+i);return d;});
  const weekStart=new Date(`${selected}T12:00:00`); weekStart.setDate(weekStart.getDate()-weekStart.getDay());
  const weekEnd=new Date(weekStart); weekEnd.setDate(weekEnd.getDate()+6);
  const visibleTasks=priorities.filter((p:Priority)=>{const d=new Date(`${taskDate(p)}T12:00:00`);return agenda==="day"?taskDate(p)===selected:d>=weekStart&&d<=weekEnd;}).sort((a:Priority,b:Priority)=>taskDate(a).localeCompare(taskDate(b))||(a.time||"99:99").localeCompare(b.time||"99:99"));
  const add=()=>{const title=text.trim();if(!title)return;const entry:Priority={id:Date.now(),text:title,date:selected,category:category.trim()||undefined,time:time||undefined,done:false};setPriorities((current:Priority[])=>[...current,entry]);setText("");setCategory("");setTime("");};
  const moveMonth=(n:number)=>setMonth(new Date(month.getFullYear(),month.getMonth()+n,1,12));
  const choose=(d:Date)=>{setSelected(localDate(d));setMonth(new Date(d.getFullYear(),d.getMonth(),1,12));};
  return <section className="panel brand-calendar" aria-label="Brand planning calendar">
    <div className="calendar-toolbar"><div><small>TEAM PLANNING · SAVED ON THIS DEVICE</small><h2>Brand calendar</h2><p>Campaigns, drops, errands, deadlines—plan it your way.</p></div><div className="month-controls"><button onClick={()=>moveMonth(-1)} aria-label="Previous month">‹</button><b>{month.toLocaleDateString("en-US",{month:"long",year:"numeric"})}</b><button onClick={()=>moveMonth(1)} aria-label="Next month">›</button><button className="today-button" onClick={()=>choose(new Date())}>Today</button></div></div>
    <div className="month-grid" role="grid">{["Sun","Mon","Tue","Wed","Thu","Fri","Sat"].map(d=><span className="weekday" key={d}>{d}</span>)}{cells.map(d=>{const value=localDate(d), dayTasks=priorities.filter((p:Priority)=>taskDate(p)===value), outside=d.getMonth()!==month.getMonth();return <button role="gridcell" aria-selected={selected===value} key={value} className={`${selected===value?"selected ":""}${outside?"outside ":""}${value===today?"today":""}`} onClick={()=>choose(d)}><b>{d.getDate()}</b><span>{dayTasks.slice(0,2).map((p:Priority)=><i className={p.done?"complete":""} key={p.id}>{p.time&&<em>{p.time} </em>}{p.text}</i>)}{dayTasks.length>2&&<small>+{dayTasks.length-2} more</small>}</span></button>})}</div>
    <div className="agenda-head"><div><small>AGENDA</small><h3>{agenda==="day"?new Date(`${selected}T12:00:00`).toLocaleDateString("en-US",{weekday:"long",month:"long",day:"numeric"}):`${weekStart.toLocaleDateString("en-US",{month:"short",day:"numeric"})} – ${weekEnd.toLocaleDateString("en-US",{month:"short",day:"numeric"})}`}</h3></div><div className="agenda-toggle"><button className={agenda==="day"?"active":""} onClick={()=>setAgenda("day")}>Day</button><button className={agenda==="week"?"active":""} onClick={()=>setAgenda("week")}>Week</button></div></div>
    <div className="calendar-add"><input className="title-input" value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==="Enter"&&add()} placeholder="Add a task or event…" aria-label="Task or event title"/><input type="time" value={time} onChange={e=>setTime(e.target.value)} aria-label="Optional time"/><input list="category-suggestions" value={category} onChange={e=>setCategory(e.target.value)} placeholder="Optional label" aria-label="Optional category"/><datalist id="category-suggestions">{["Sourcing","Listings","Shipping","Content","Operations","Campaign","Finance","Studio"].map(c=><option key={c} value={c}/>)}</datalist><button onClick={add}>＋ Add</button></div>
    <div className="calendar-tasks">{visibleTasks.length===0?<div className="agenda-empty"><b>Open space</b><p>Nothing planned here yet. Add any task, event, reminder, or milestone above.</p></div>:visibleTasks.map((p:Priority)=><article key={p.id} className={p.done?"is-done":""}><input type="checkbox" checked={p.done} onChange={()=>setPriorities(priorities.map((x:Priority)=>x.id===p.id?{...x,date:taskDate(x),done:!x.done}:x))} aria-label={`Complete ${p.text}`}/><div>{editing===p.id?<input className="edit-input" autoFocus value={editText} onChange={e=>setEditText(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&editText.trim()){setPriorities(priorities.map((x:Priority)=>x.id===p.id?{...x,text:editText.trim(),date:taskDate(x)}:x));setEditing(null)}if(e.key==="Escape")setEditing(null)}}/>:<button className="task-title" onClick={()=>{setEditing(p.id);setEditText(p.text)}}>{p.text}</button>}<span>{agenda==="week"&&new Date(`${taskDate(p)}T12:00:00`).toLocaleDateString("en-US",{weekday:"short"})}{p.time&&` · ${new Date(`2000-01-01T${p.time}`).toLocaleTimeString("en-US",{hour:"numeric",minute:"2-digit"})}`}{p.category&&<em>{p.category}</em>}</span></div><button onClick={()=>{setEditing(p.id);setEditText(p.text)}} aria-label={`Edit ${p.text}`}>Edit</button><button className="delete" onClick={()=>setPriorities(priorities.filter((x:Priority)=>x.id!==p.id))} aria-label={`Delete ${p.text}`}>×</button></article>)}</div>
    <footer className="calendar-note"><span>● Local workspace</span><p>Designed for a small team to plan together on one device. Shared accounts and live sync aren’t connected yet.</p></footer>
  </section>;
}
function Metric({label,value}:{label:string,value:string|number}){return <div><span>{label}</span><b>{value}</b><small>Based on your records</small></div>}
function Empty({icon,title,copy,action,label}:{icon:string,title:string,copy:string,action?:()=>void,label?:string}){return <div className="empty"><span>{icon}</span><h2>{title}</h2><p>{copy}</p>{action&&<button onClick={action}>{label}</button>}</div>}
function Inventory({items,setItems,start,notify}:any){const owned=items.filter((i:Item)=>i.status==="Owned");return <div className="page section"><Title eyebrow="OWNED STOCK" title="Inventory" copy="Everything you own and have not yet listed for sale." action={start} actionLabel="◎ Start thrift session"/>{owned.length===0?<Empty icon="▦" title="Your inventory is empty" copy="Start a thrift session to photograph and log your first find." action={start} label="Start thrift session"/>:<ItemList items={owned} actionLabel="Ready to list" action={(item:Item)=>{setItems(items.map((i:Item)=>i.id===item.id?{...i,status:"Ready"}:i));notify("Moved to Listings");}}/>}</div>}
function Listings({items,setItems,notify}:any){const list=items.filter((i:Item)=>["Ready","Published"].includes(i.status));return <div className="page section"><Title eyebrow="READY TO SELL" title="Listings" copy="Prepare items and track what you publish."/>{list.length===0?<Empty icon="▤" title="No listings yet" copy="Mark an inventory item ready to list and it will appear here."/>:<ItemList items={list} actionLabel="Mark published" action={(item:Item)=>{setItems(items.map((i:Item)=>i.id===item.id?{...i,status:"Published"}:i));notify("Marked published locally");}}/>}</div>}
function Orders({items,setItems,notify}:any){const orders=items.filter((i:Item)=>i.status==="Sold");return <div className="page section"><Title eyebrow="SOLD + FULFILLMENT" title="Orders" copy="Sold items and fulfillment status live here."/>{orders.length===0?<Empty icon="↗" title="No orders yet" copy="Your sold items will appear here for fulfillment."/>:<ItemList items={orders} actionLabel="Mark fulfilled" action={()=>notify("Fulfillment saved locally")}/>}<details className="dev-control"><summary>Local review control</summary><p>To test Orders, mark a published listing sold from its item menu.</p></details></div>}
function Finances({items,revenue,costs,taxRate,setTaxRate,taxPayments,setTaxPayments}:any){
  const sold=items.filter((i:Item)=>i.status==="Sold"), profit=sold.reduce((n:number,i:Item)=>n+i.price-i.cost,0);
  const [payment,setPayment]=useState("");
  const estimatedReserve=Math.max(0,profit)*(Math.max(0,Number(taxRate)||0)/100), paid=taxPayments.reduce((n:number,p:TaxPayment)=>n+p.amount,0), setAside=Math.max(0,estimatedReserve-paid);
  const recordPayment=(e:FormEvent)=>{e.preventDefault();const amount=Number(payment);if(amount<=0)return;setTaxPayments([...taxPayments,{id:Date.now(),amount,date:localDate()}]);setPayment("");};
  return <div className="page section"><Title eyebrow="CASH + PROFIT" title="Finances" copy="A simple view of money recorded in VIGILKLINE."/><section className="metrics"><Metric label="Revenue" value={money(revenue)}/><Metric label="Inventory spend" value={money(costs)}/><Metric label="Net profit" value={money(profit)}/><Metric label="Cash received" value={money(revenue)}/></section>
    <section className="panel tax-panel"><div className="panel-head"><div><small>LOCAL ESTIMATE</small><h2>Tax set-aside</h2><p>Simple planning based on recorded net profit. This is an estimate, not tax advice.</p></div><label>Estimated tax rate <span><input type="number" min="0" max="100" step="0.1" value={taxRate} onChange={e=>setTaxRate(e.target.value)}/>%</span></label></div><div className="tax-summary"><div><small>ESTIMATED TAX RESERVE</small><b>{money(estimatedReserve)}</b><span>{Number(taxRate)||0}% of positive recorded profit</span></div><div><small>TAX PAYMENTS RECORDED</small><b>{money(paid)}</b><span>Entered manually on this device</span></div><div className="tax-balance"><small>SET ASIDE FOR TAXES</small><b>{money(setAside)}</b><span>Estimated reserve minus recorded payments</span></div></div><form className="tax-payment" onSubmit={recordPayment}><label>Record a tax payment $<input type="number" min="0.01" step="0.01" value={payment} onChange={e=>setPayment(e.target.value)} placeholder="0.00"/></label><button disabled={!payment||Number(payment)<=0}>Record payment</button></form>{taxPayments.length>0&&<div className="tax-history">{taxPayments.map((p:TaxPayment)=><div key={p.id}><span>Tax payment · {new Date(`${p.date}T12:00:00`).toLocaleDateString("en-US",{month:"short",day:"numeric",year:"numeric"})}</span><b>−{money(p.amount)}</b></div>)}</div>}<footer>No banking or tax systems are connected. Confirm tax obligations with a qualified professional.</footer></section>
    {items.length===0?<Empty icon="$" title="No financial activity" copy="Costs appear when you add finds. Revenue, profit, and estimated taxes appear when items sell."/>:<div className="panel ledger"><div className="panel-head"><div><small>LEDGER</small><h2>Recorded activity</h2></div></div>{items.map((i:Item)=><div key={i.id}><span>{i.brand||"Unbranded"} · {i.description}</span><b>{i.status==="Sold"?`+${money(i.price-i.cost)}`:`−${money(i.cost)}`}</b></div>)}</div>}</div>}
function Title({eyebrow,title,copy,action,actionLabel}:any){return <section className="title"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{copy}</p></div>{action&&<button className="start" onClick={action}>{actionLabel}</button>}</section>}
function ItemList({items,action,actionLabel}:any){return <div className="item-list">{items.map((i:Item)=><article key={i.id}>{i.photo?<img src={i.photo} alt=""/>:<div className="no-photo">VK</div>}<div><small>{i.status.toUpperCase()} · #{String(i.id).slice(-6)}</small><h3>{i.brand||"Unbranded"}</h3><p>{i.description||"No description"}</p></div><span><b>{money(i.cost)}</b><small>cost</small></span><button onClick={()=>action(i)}>{actionLabel} →</button></article>)}</div>}

function PastSessions({sessions}:{sessions:ThriftHistory[]}){
  const [open,setOpen]=useState<number|null>(sessions[0]?.id??null);
  return <div className="page section"><Title eyebrow="SOURCING HISTORY" title="Past thrift sessions" copy="Bought and passed finds stay together here for later reference."/>{sessions.length===0?<Empty icon="◷" title="No past sessions" copy="Finish a thrift session and its cart, timing, and decisions will be saved on this device."/>:<div className="session-history">{sessions.map(s=>{const bought=s.candidates.filter(c=>c.decision==="bought"),passed=s.candidates.filter(c=>c.decision==="passed");return <article className="history-card" key={s.id}><button className="history-summary" onClick={()=>setOpen(open===s.id?null:s.id)}><div><small>{new Date(s.date+"T12:00:00").toLocaleDateString("en-US",{month:"short",day:"numeric",year:"numeric"})}</small><h2>{s.location||"Location not entered"}</h2><p>{bought.length} bought · {passed.length} passed · {formatDuration(s.phaseTimes.store)} in store</p></div><div className="history-thumbs">{s.candidates.slice(0,4).map(c=>c.photo?<img key={c.id} src={c.photo} alt=""/>:<span key={c.id}>VK</span>)}</div><strong>{money(s.spend)}<small> of {money(s.budget)}</small></strong><i>{open===s.id?"−":"+"}</i></button>{open===s.id&&<div className="history-detail"><div className="history-metrics"><span><small>TOTAL TIME</small><b>{formatDuration(s.phaseTimes.driving+s.phaseTimes.parking+s.phaseTimes.store)}</b></span><span><small>IN STORE</small><b>{formatDuration(s.phaseTimes.store)}</b></span><span><small>DRIVING</small><b>{formatDuration(s.phaseTimes.driving)}</b></span><span><small>TRANSITION</small><b>{formatDuration(s.phaseTimes.parking)}</b></span></div><h3>Bought</h3><HistoryCandidates candidates={bought}/><h3>Passed</h3><HistoryCandidates candidates={passed}/></div>}</article>})}</div>}</div>;
}
function HistoryCandidates({candidates}:{candidates:SessionCandidate[]}){return candidates.length?<div className="history-candidates">{candidates.map(c=><div key={c.id}>{c.photo?<img src={c.photo} alt=""/>:<span>VK</span>}<p><b>{c.brand||"Unbranded"}</b><small>{c.description||"No notes"}</small></p><strong>{money(c.cost)}</strong></div>)}</div>:<p className="history-empty">None</p>}

function ThriftSession({close,checkpoint,commit}:{close:()=>void;checkpoint:(session:ThriftHistory,items:Item[])=>void;commit:(session:ThriftHistory,items:Item[])=>void}){
  const [cart,setCart]=useState<SessionCandidate[]>([]), [draft,setDraft]=useState({brand:"",description:"",cost:"",resale:""}), [photo,setPhoto]=useState("");
  const [analysis,setAnalysis]=useState<"idle"|"working"|"ready"|"error">("idle"), [analysisMessage,setAnalysisMessage]=useState("");
  const [budget,setBudget]=useState("100"), [now,setNow]=useState(()=>Date.now()), [captureOpen,setCaptureOpen]=useState(false);
  const [location,setLocation]=useState(""), [startedAt,setStartedAt]=useState(()=>Date.now()), [reviewAction,setReviewAction]=useState<"finish"|"location"|null>(null), [nextLocation,setNextLocation]=useState("");
  const [phase,setPhase]=useState<SessionPhase|null>(null), [phaseStartedAt,setPhaseStartedAt]=useState<number|null>(null), [phaseTimes,setPhaseTimes]=useState<Record<SessionPhase,number>>({driving:0,parking:0,store:0});
  const bought=cart.filter(i=>i.decision==="bought"), total=useMemo(()=>bought.reduce((n,i)=>n+i.cost,0),[bought]), projected=useMemo(()=>bought.reduce((n,i)=>n+(i.estimatedResale||0),0),[bought]);
  const potential=projected-total, margin=projected>0?Math.round((potential/projected)*100):0, remaining=(Number(budget)||0)-total;
  const liveTimes=useMemo(()=>{const times={...phaseTimes};if(phase&&phaseStartedAt)times[phase]+=Math.max(0,Math.floor((now-phaseStartedAt)/1000));return times;},[phaseTimes,phase,phaseStartedAt,now]);
  const elapsed=liveTimes.driving+liveTimes.parking+liveTimes.store, elapsedLabel=formatDuration(elapsed);
  useEffect(()=>{Promise.all([read("active-session"),read("active-session-meta")]).then(([v,m])=>{if(Array.isArray(v))setCart((v as Partial<SessionCandidate>[]).map(i=>({...i,decision:i.decision||"undecided"} as SessionCandidate)));if(m&&typeof m==="object"){if(m.budget)setBudget(String(m.budget));if(m.location)setLocation(String(m.location));if(m.startedAt)setStartedAt(Number(m.startedAt));if(m.phaseTimes)setPhaseTimes({...{driving:0,parking:0,store:0},...m.phaseTimes});}}).catch(()=>{});const timer=window.setInterval(()=>setNow(Date.now()),1000);return()=>window.clearInterval(timer);},[]);
  useEffect(()=>{write("active-session",cart).catch(()=>{});},[cart]);
  useEffect(()=>{write("active-session-meta",{budget,location,startedAt,phaseTimes:liveTimes}).catch(()=>{});},[budget,location,startedAt,liveTimes]);
  const changePhase=(next:SessionPhase|null)=>{if(next==="driving"&&reviewAction==="location"){changeLocation();return}const updated={...phaseTimes};if(phase&&phaseStartedAt)updated[phase]+=Math.max(0,Math.floor((Date.now()-phaseStartedAt)/1000));setPhaseTimes(updated);setPhase(next);setPhaseStartedAt(next?Date.now():null);};
  async function analyzePhoto(imageDataUrl:string){setAnalysis("working");setAnalysisMessage("Checking the photo…");try{const response=await fetch("/api/garment-analysis",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({imageDataUrl})});const result=await response.json() as {suggestion?:{brand?:string;description?:string;tagPrice?:number|null};error?:string};if(!response.ok||!result.suggestion)throw new Error(result.error||"AI check could not finish.");const s=result.suggestion;setDraft(current=>({...current,brand:s.brand||current.brand,description:s.description||current.description,cost:s.tagPrice!=null&&current.cost===""?String(s.tagPrice):current.cost}));setAnalysis("ready");setAnalysisMessage(s.tagPrice!=null?"Suggestion added — confirm the tag price before saving.":"Suggestion added — verify it before saving.");}catch(error){setAnalysis("error");setAnalysisMessage(error instanceof Error?error.message:"AI check could not finish.");}}
  function capture(e:ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{const imageDataUrl=String(reader.result);setPhoto(imageDataUrl);analyzePhoto(imageDataUrl);};reader.readAsDataURL(file);}
  function add(e:FormEvent){e.preventDefault();if(!photo||!draft.cost)return;setCart(v=>[{id:Date.now(),brand:draft.brand.trim(),description:draft.description.trim(),cost:Number(draft.cost)||0,price:0,estimatedResale:Number(draft.resale)||undefined,decision:"undecided",photo},...v]);setDraft({brand:"",description:"",cost:"",resale:""});setPhoto("");setAnalysis("idle");setAnalysisMessage("");setCaptureOpen(false);}
  const decide=(id:number,decision:CandidateDecision)=>setCart(v=>v.map(i=>i.id===id?{...i,decision}:i));
  const openReview=(action:"finish"|"location")=>{changePhase(null);setReviewAction(action);};
  const reviewedRecords=()=>{const endedAt=Date.now();const session:ThriftHistory={id:endedAt,date:localDate(),startedAt,endedAt,location:location.trim(),budget:Number(budget)||0,spend:total,phaseTimes:{...liveTimes},candidates:cart};const inventory:Item[]=bought.map(i=>({id:i.id,brand:i.brand,description:i.description,cost:i.cost,price:i.price,estimatedResale:i.estimatedResale,photo:i.photo,status:"Owned"}));return {session,inventory}};
  const finish=()=>{const {session,inventory}=reviewedRecords();write("active-session",[]).catch(()=>{});write("active-session-meta",undefined).catch(()=>{});commit(session,inventory);};
  const changeLocation=()=>{const {session,inventory}=reviewedRecords();checkpoint(session,inventory);const nextStarted=Date.now();setCart([]);setLocation(nextLocation.trim());setStartedAt(nextStarted);setPhaseTimes({driving:0,parking:0,store:0});setPhase("driving");setPhaseStartedAt(nextStarted);setReviewAction(null)};
  return <div className="session-screen" role="dialog" aria-modal="true"><header><button onClick={()=>{changePhase(null);close()}}>‹</button><div><small>LIVE SESSION</small><b>Thrift session</b></div><span><i className={phase?"active":""}/> {elapsedLabel} · {phase?phaseLabel(phase):"Paused"}</span></header><div className="cockpit">
    <section className="session-overview"><div><p className="eyebrow">SESSION ACTIVE</p><h1>Build your cart as you shop.</h1><p>A photo adds a candidate to this session—not to inventory. Mark what you actually bought before finishing.</p></div><button className="capture-cta" onClick={()=>setCaptureOpen(true)}>◎ Capture a candidate</button></section>
    <section className="session-location"><label>Store / location<input value={location} onChange={e=>setLocation(e.target.value)} placeholder="Optional, e.g. Goodwill on Sunset"/></label><button onClick={()=>{setNextLocation("");openReview("location")}}>Review & change location →</button></section>
    <section className="phase-timer"><div className="phase-current"><small>MANUAL TIME TRACKER</small><b>{phase?phaseLabel(phase):"Timer paused"}</b><span>{elapsedLabel} total · saved on this device</span></div><div className="phase-controls">{(["driving","parking","store"] as SessionPhase[]).map(p=><button key={p} className={phase===p?"active":""} onClick={()=>changePhase(p)}>{phase===p?"● ":""}{phaseLabel(p)}</button>)}<button className="pause" disabled={!phase} onClick={()=>changePhase(null)}>Ⅱ Pause</button></div><div className="phase-summary"><span><b>{formatDuration(liveTimes.store)}</b> In store</span><span><b>{formatDuration(liveTimes.driving)}</b> Driving</span><span><b>{formatDuration(liveTimes.parking)}</b> Parking / transition</span><strong>{formatDuration(liveTimes.driving+liveTimes.parking)} travel vs {formatDuration(liveTimes.store)} in store</strong></div><p>Timing is controlled by you; no location tracking is used.</p></section>
    <section className="cockpit-metrics"><div><small>BOUGHT SPEND</small><b>{money(total)}</b><span>{bought.length} bought · {cart.filter(i=>i.decision==="undecided").length} to review</span></div><div><small>BUDGET LEFT</small><b className={remaining<0?"warning":""}>{money(remaining)}</b><label>Budget $<input type="number" min="0" step="1" value={budget} onChange={e=>setBudget(e.target.value)}/></label></div><div><small>EST. RESALE*</small><b>{money(projected)}</b><span>Bought items only</span></div><div><small>POTENTIAL PROFIT*</small><b>{money(potential)}</b><span>{margin}% projected margin</span></div></section>
    {remaining<0&&<div className="session-alert danger"><b>Budget exceeded</b><p>You’re {money(Math.abs(remaining))} over your session budget. Review before adding another find.</p></div>}
    <div className="session-guidance"><span>◇ MANUAL REVIEW</span><p>Verify condition and your resale assumptions. Market pricing, brand recognition, and sell-through data are not connected.</p></div>
    <section className="cockpit-grid"><div className="finds-card"><div className="finds-head"><div><small>LIVE CART</small><h2>Session candidates</h2></div><button onClick={()=>setCaptureOpen(true)}>＋ Add candidate</button></div>{cart.length===0?<div className="empty-cart"><span>◇</span><b>Your cart is empty</b><p>Capture a photo and enter the price tag to start.</p></div>:<div className="session-items">{cart.map(i=>{const profit=(i.estimatedResale||0)-i.cost;return <article className={`candidate ${i.decision}`} key={i.id}><img src={i.photo} alt=""/><div><b>{i.brand||"Unbranded"}</b><p>{i.description||"No description"}</p><small>{i.estimatedResale?`${money(i.estimatedResale)} est. resale · ${money(profit)} potential`:"No resale estimate"}</small><div className="decision-buttons"><button className={i.decision==="bought"?"active":""} onClick={()=>decide(i.id,"bought")}>✓ Bought / Keep</button><button className={i.decision==="passed"?"active":""} onClick={()=>decide(i.id,"passed")}>Passed / Drop</button></div></div><strong>{money(i.cost)}</strong><button onClick={()=>setCart(cart.filter(x=>x.id!==i.id))} aria-label="Remove candidate">×</button></article>})}</div>}</div></section>
    <footer className="session-finish"><p>* Projections use bought items only. Passed items remain in session history and never enter inventory.</p><button disabled={!cart.length} onClick={()=>openReview("finish")}>Review cart & finish <span>{cart.length} candidates</span></button></footer>
  </div>{reviewAction&&<div className="review-backdrop"><section className="cart-review" role="dialog" aria-modal="true"><div className="sheet-head"><div><small>CART REVIEW</small><b>{reviewAction==="finish"?"Review before finishing":"Review before changing location"}</b></div><button onClick={()=>setReviewAction(null)}>×</button></div><p>Mark every candidate. Only bought items move to inventory; passed items stay in this session’s history.</p><div className="review-list">{cart.map(i=><article key={i.id}><img src={i.photo} alt=""/><span><b>{i.brand||"Unbranded"}</b><small>{money(i.cost)}</small></span><div className="decision-buttons"><button className={i.decision==="bought"?"active":""} onClick={()=>decide(i.id,"bought")}>✓ Bought</button><button className={i.decision==="passed"?"active":""} onClick={()=>decide(i.id,"passed")}>Passed</button></div></article>)}</div>{reviewAction==="location"&&<label className="next-location">Next store / location<input autoFocus value={nextLocation} onChange={e=>setNextLocation(e.target.value)} placeholder="Optional location label"/></label>}<div className="review-total"><span>{bought.length} bought · {cart.filter(i=>i.decision==="passed").length} passed</span><b>{money(total)} spend</b></div><button className="review-confirm" disabled={cart.some(i=>i.decision==="undecided")} onClick={()=>{if(reviewAction==="finish")finish();else{setLocation(nextLocation.trim());setReviewAction(null);changePhase("driving")}}}>{cart.some(i=>i.decision==="undecided")?`${cart.filter(i=>i.decision==="undecided").length} still need a decision`:reviewAction==="finish"?`Move ${bought.length} to inventory & save session`:"Save review & start next location"}</button></section></div>}{captureOpen&&<div className="capture-sheet" role="dialog" aria-modal="true" aria-label="Capture a candidate"><div className="sheet-head"><div><small>NEW CANDIDATE</small><b>Add to cart for review</b></div><button onClick={()=>setCaptureOpen(false)}>×</button></div><div className="camera-frame compact">{photo?<img src={photo} alt="New candidate preview"/>:<div><span>◎</span><b>Photograph the item</b><p>This photo stays in the session cart until you decide.</p></div>}<label>{photo?"Retake":"Open camera"}<input type="file" accept="image/*" capture="environment" onChange={capture}/></label></div><div className={`scan-state ai-${analysis}`}><span>{analysis==="working"?"AI CHECKING":analysis==="ready"?"AI SUGGESTION":analysis==="error"?"AI UNAVAILABLE":"AI READY"}</span><p>{analysisMessage||"Take a photo to get editable brand, description, and readable tag-price suggestions. Always verify before saving."}</p>{photo&&analysis!=="working"&&<button type="button" className="analyze-photo" onClick={()=>analyzePhoto(photo)}>{analysis==="error"?"Try AI check again":"Check photo again"}</button>}</div><form className="capture-form" onSubmit={add}><label>Brand / maker<input value={draft.brand} onChange={e=>setDraft({...draft,brand:e.target.value})} placeholder="Optional"/></label><label>Item notes<input value={draft.description} onChange={e=>setDraft({...draft,description:e.target.value})} placeholder="Condition, size, style…"/></label><label>Tag price *<input inputMode="decimal" type="number" min="0" step=".01" value={draft.cost} onChange={e=>setDraft({...draft,cost:e.target.value})} placeholder="0.00"/></label><label>Your resale estimate<input inputMode="decimal" type="number" min="0" step=".01" value={draft.resale} onChange={e=>setDraft({...draft,resale:e.target.value})} placeholder="Optional"/></label>{draft.cost&&draft.resale&&<div className={`intake-tip ${Number(draft.resale)-Number(draft.cost)<=Number(draft.resale)*.25?"danger":""}`}><b>{money(Number(draft.resale)-Number(draft.cost))} potential before fees</b><span>{Math.round(((Number(draft.resale)-Number(draft.cost))/Number(draft.resale))*100)}% projected margin from your estimate</span></div>}<button disabled={!photo||!draft.cost||analysis==="working"}>{analysis==="working"?"Checking photo…":"Add candidate to cart"}</button></form></div>}</div>;
}
const formatDuration=(seconds:number)=>`${String(Math.floor(seconds/3600)).padStart(2,"0")}:${String(Math.floor((seconds%3600)/60)).padStart(2,"0")}:${String(seconds%60).padStart(2,"0")}`;
const phaseLabel=(phase:SessionPhase)=>phase==="driving"?"Driving":phase==="parking"?"Parking / transition":"Inside store";
