# Copy this file to .dev.vars. It is intentionally ignored by Git.
# Never put this key in the browser, source code, or chat.
OPENAI_API_KEY=replace_with_your_saved_key

# Optional. The garment analysis route defaults to GPT-5.6 Luna.
# OPENAI_MODEL=gpt-5.6-luna
